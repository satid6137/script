ยังไม่มี Composer
ให้ติดตั้งด้วยคำสั่งนี้:

Code
sudo apt install composer
หรือถ้าเป็น CentOS/AlmaLinux:

Code
sudo dnf install composer
แล้วค่อยรัน:

Code
cd /opt/lampp/htdocs/script
composer require vlucas/phpdotenv